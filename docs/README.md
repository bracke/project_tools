# project_tools API documentation

Generate API documentation with GNATdoc from this directory:

```sh
gnatdoc -P api.gpr -O api
```

The generated HTML is written to `docs/api/` and is intentionally not required for normal builds.
