with "config/project_tools_config.gpr";
library project Project_Tools is

   --  Per-OS backend directory, as cryptolib does: "is this a link" cannot be
   --  answered portably (see Project_Tools.Links).
   for Source_Dirs use ("src/", "src-" & Project_Tools_Config.Alire_Host_OS & "/");
   for Object_Dir use "obj/" & Project_Tools_Config.Build_Profile;
   for Library_Dir use "lib";
   for Library_Name use "project_tools";
   for Library_Kind use "static";
   for Create_Missing_Dirs use "True";

   package Compiler is
      for Default_Switches ("Ada") use
        Project_Tools_Config.Ada_Compiler_Switches & ("-gnat2022", "-gnatyM120", "-gnatX");
   end Compiler;

end Project_Tools;
